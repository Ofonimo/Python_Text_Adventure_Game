# ANGELS AND DEMONS TEXT ADVENTURE GAMEimport random # importing random packageimport time #importing time package#defining a dice function to improve replay value#if the user is lucky, they skip stage 3def dice():        roll_dice = 3 #adding a counter to control the invalid inputs            while roll_dice > 0: #condition for the while loop to run        #getting user input        user_input = input(prompt = """            The guard at the door wants you to roll a dice once before you proceed.    If you roll a double, you get to skip Stage 3. Do you want to roll?\n    1. yes    2. no """)                #converting user input to lower case and no spaces        user_input = user_input.lower().strip()        if '1' in user_input or 'yes' in user_input or 'y' in user_input:            dice_1 = random.randint(1,6)            dice_2 = random.randint(1,6)            print(f"""    Rolling the dice....................""")            time.sleep(1)            print(f"""     Dice 1 = {dice_1}     Dice 2 = {dice_2}""")                        if dice_1 == dice_2:                print("""    You rolled a double. CONGRATULATIONS!!!""")                win()                play_again_3_win()                break                        else:                print("""    Sorry! You didn't roll a double. Please proceed to stage 3""")                want_to_cont()                break                        elif '2' in user_input or 'no' in user_input or 'n' in user_input:            print("\n That's alright. Please proceed to stage 3.")            want_to_cont()            break                    else:            print("\n Invalid response!!! Please enter yes/no \n")            # number of attempts decreases by 1 each time the user enters invalid answer            roll_dice -= 1            if roll_dice != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {roll_dice} attempt(s) left""")            else:                end()                break                                                                  #defining an exit function to be called each time the user decides to end the game#The ASCII text was gotten from https://patorjk.com/software/taag/#p=display&f=Graffiti&t=Type%20Something%20def end():    print("""\n    ___              _ _             _ _ _   / __|___  ___  __| | |__ _  _ ___| | | | | (_ / _ \/ _ \/ _` | '_ \ || / -_)_|_|_|  \___\___/\___/\__,_|_.__/\_, \___(_|_|_)                           |__/           """)               # defining a win function that appears each time the user winsdef win():    print(""" \n  __   __                 _      _ _ _  \ \ / /__ _  _  __ __ _(_)_ _ | | | |  \ V / _ \ || | \ V  V / | ' \|_|_|_|   |_|\___/\_,_|  \_/\_/|_|_||_(_|_|_)                                       """)              #defining a lose function that appears each time the user loses a game   def lose():    print("""\n  __   __          _             _    ___                                  _ _ _  \ \ / /__ _  _  | |___ ___ ___| |  / __|__ _ _ __  ___   _____ _____ _ _| | | |  \ V / _ \ || | | / _ (_-</ -_)_| | (_ / _` | '  \/ -_) / _ \ V / -_) '_|_|_|_|   |_|\___/\_,_| |_\___/__/\___(_)  \___\__,_|_|_|_\___| \___/\_/\___|_| (_|_|_)                                                                                """)            # defining a play_again function to be called when the user fails a stage#This will ask them if they want to try again.def play_again_1():        invalid_input = 3 #adding a counter to control the invalid inputs        while invalid_input > 0: #condition for the while loop to run                #getting user input        user_input = input(prompt = """        Do you want to try again?\n        1. yes        2. no """)                #converting user input to lower case and no spaces        user_input = user_input.lower().strip()        if '1' in user_input or 'yes' in user_input or 'y' in user_input:            stage_1() #calling stage_1 if the user wants to play again            break                    elif '2' in user_input or 'no' in user_input or 'n' in user_input:            print("\n Thanks for playing. Have a nice day!!!")            end()            break                    else:            print("\n Invalid response!!! Please enter yes/no \n")            # number of attempts decreases by 1 each time the user enters invalid answer            invalid_input -= 1            if invalid_input != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {invalid_input} attempt(s) left""")            else:                end()                break                                                                   #creating a play again func that will be called at the end of stage 3#If the user decides to play again, they will start from stage 1def play_again_3_lose():            input_invalid = 3 #adding a counter to control the invalid inputs        while input_invalid > 0: #condition for the while loop to run                #getting user input        user_input = input(prompt = """        Do you want to try again?\n        1. yes        2. no """)                                #converting user input to lower case and no spaces        user_input = user_input.lower().strip()                if '1' in user_input  or 'yes' in user_input or 'y' in user_input :            intro()            break                    elif '2' in user_input or 'no' in user_input or 'n' in user_input :            print("\n Thanks for playing. Have a nice day!!!\n")            end()            break                    else:            print("\n Invalid response!!! Please enter yes/no \n")                        # number of attempts decreases by 1 each time the user enters invalid answer            input_invalid -= 1            if input_invalid != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {input_invalid} attempt(s) left""")            else:                end()                break                                    #defining a play_function for when the user wins the game.#this is to ask if they want to play the game againdef play_again_3_win():    response = 3 #adding a counter to control the invalid inputs        while response > 0: #condition for the while loop to run                user_input = input(prompt = """        Do you want to play this AWESOME game again?\n        1. yes        2. no """)                                #converting user input to lower case and no spaces        user_input = user_input.lower().strip()        if '1' in user_input or 'yes' in user_input or 'y' in user_input :                        intro() #calling introduction function            break                    elif '2' in user_input or 'no' in user_input or 'n' in user_input :            print('\n Thanks for playing. Have a nice day!!!')            end()            break                    else:            print("\n Invalid response!!! Please enter yes/no \n")                        # number of attempts decreases by 1 each time the user enters invalid answer            response -= 1            if response != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {response} attempt(s) left""")            else:                end()                break                                                  #defining the function asking the user if they want to play #in order to call it later.   def stage_1():        entries = 3 #adding a counter to control the invalid inputs        while entries > 0: #condition for the while loop to run                #asking the user if they want to play        user_input = input(prompt = f"""    Would you like to play Angels and Demons game?(Yes/No):\n    1. yes    2. no   """)                        #converting user input to lower case and no spaces        user_input = user_input.lower().strip()        #if statement based on user's input.         #making the game user friendly by simplifying user input        #if the user wants to play, we proceed                if 'yes' in user_input or 'y' in user_input or '1' in user_input :             print("""    The pope has been killed, the cannister stolen, and a physicist murdered all     within the last couple of hours.\n\n    The cannister will explode in 24 hours if the battery is not changed in time.            The Swiss guard needs your help to solve the mystery in the Vatican City.\n""")            #calling the help_guard function instead of typing it out again.            help_guard()            break                                 #elif statement to end the game if the user doesn't want to play         elif 'no' in user_input or '2' in user_input or 'n' in user_input :             print(f"""    Too bad!!! I was really hoping we could play!(""")            end()            break #exiting the game                   #else statement incase the user enters invalid answer            else:            print("""\nYou entered an invalid answer!!! Please enter yes/no \n""")                        # number of attempts decreases by 1 each time the user enters invalid answer            entries -= 1            #ent_attempts += 1            if entries != 0:                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {entries} attempt(s) left""")            else:                end()                break        #defining help_guard function to call it later.        def help_guard():        attempt_count = 3 #adding a counter to control the invalid inputs    while attempt_count > 0: #condition for the while loop to run                #getting user input         user_input = input(prompt = f"""    Will you help the Swiss guards? \n    1. yes    2. no  """)                        # making sure everything is converted to lower case and no spaces        user_input = user_input.lower().strip()        #if statement giving a condition to execute if the user enter's 'yes'        #making the game user friendly by simplifying user input        if 'yes' in user_input or 'y' in user_input or '1' in user_input:                                    print("""    Welcome to Vatican City!!!    We need to access the Vatican archives in order to find a ÒBound volumeÓ\n    The Bound has coded messages that will lead us to The Church of Illumination,    we received an intel that the cannister is hidden there. \n    We can either get permission from the Camerlengo or we can sneak in, to save time.\n            """)            permission_or_sneak() # calling permission_or_sneak function when user decides to help            break                        #elif statement to end the game if the user doesn't want to play.        elif 'no' in user_input or 'n' in user_input or '2' in user_input:                                                 print(f"""    Too bad!!! I was really hoping we could play! :(""")            end()            break                   #else statement incase the user enters an invalid answer           else:            print("""\nYou entered an invalid answer!!! Please enter yes/no\n""")            attempt_count -= 1            if attempt_count != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {attempt_count} attempt(s) left""")            else:                end()                break                                #defining a function for the part that asks for permission, so I can call it instead of rewriting it.def permission_or_sneak():        count_tries = 3 #adding a counter to control the invalid inputs            while count_tries > 0: #condition for the while loop to run                #generating a random number between 1 and 10 for the user to guess        #Making sure the game is not predictable by including random        number = random.randint(a=1, b= 10)               tries = 3 #setting limits        guess_count = 0 # Counting the number of guesses        guess_limit = 3 # Setting a limit. The user has only 3 tries before running out of time        stage_pass = False # Boolean variable to control while loop                #adding a variable to control the number of tries the user can enter anything other than an int.        try_count = 3                 #getting user's input if they want to take permission or sneak into the archives        user_input = input(prompt = """    Input(permission/sneak)     1. permission    2. sneak """)                #converting the input to lower case and no spaces        user_input = user_input.lower().strip()        #if they want to take permission, then they proceed to the next level        #making the game user friendly by simplifying input        if 'permission' in user_input or '1' in user_input or 'perm' in user_input or 'pe' in user_input :                                    print(f"""    Nice!!!    Now we have permission to access the archieves lets go get the keys!!! \n""")            #printing information for the next step            print(""""    You have been given a bunch of keys, you need to pick the right key to open    the archives door in order to access the Bound.\n                        """)                        #creating a while loop guessing game for the user to pick a key                        while guess_count <= guess_limit and try_count > 0:#setting a condition for while loop             #Asking the user to guess a number between 1 and 10                user_input = input(prompt = """     To pick a key, Input a number between 1 and 10:   """)                try: #error control using try                                         #converting user_input to an integar                    user_input = int(user_input)                #printing out a statement for the user to enter a number if an int was not entered at the prompt.                    except:                    print("""    This is not a whole number. Please enter a number between 1 and 10. Try again!!! \n                    """)                    #number of try_count decreases by 1(the user has 3 attempts)                    try_count -=1                     continue #this will send the user back to user input                #if the user guessed right, they are through to the next level                if user_input == number:                    #if the bolean condition is true, we break the loop                    stage_pass = True                    break #break the loop when the user selects the correct answer                else: #else continue if they guessed wrong                                        #a condition for the loop to continue as long as guess_count < 3                    if guess_count < 3:                                                 #telling user the number of tries they have left                        print(f"""    Try again!! you have {tries} attempt(s) left                                """)                    else:                         print ("You ran out of tries!!!")                                        guess_count += 1 # number of gueses increases by 1 each time the user tries                tries -= 1 #number of tries decreases by 1 each time the user tries            #nested if statement to print when the stage pass is true            if stage_pass:                print("""    Good job, you are through to the next level!!!    """)                win()# calling win function                print(f""" {'*' * 60}      ____ _____  _    ____ _____   ____   / ___|_   _|/ \  / ___| ____| |___ \  \___ \ | | / _ \| |  _|  _|     __) |  ___) || |/ ___ \ |_| | |___   / __/  |____/ |_/_/   \_\____|_____| |_____|                                      {'*' * 60} """)                                stage_2()                break             else:                 lose()#calling lose functions                                #Calling play_again_1 function to ask if the user want to play again after they lose.                play_again_1()                 break #breaking the loop                    #elif the user decides to sneak into the vatican archives, they will be jailed           elif 'sneak' in user_input or '2' in user_input or 'sn' in user_input or 'sne' in user_input:            print("""    You've been arreseted by the Swiss Guard and sentenced to 6 months in jail.                 """)            lose()#calling the lose function            #Calling play_again_1 function to ask if the user want to play again after they lose.            play_again_1()            break        #If the user enters anything other than permission/sneak, they can try again.         else:            print ('\nInvalid input!! please enter (permission/sneak)\n')             count_tries -= 1 #count_tries decreases by 1 each time the user enters invalid answer            if count_tries != 0:                print(f"""    I was expecting permission or sneak (1 or 2). You have {count_tries} attempt(s) left""")            else:                end()                break                                                                #defining a function to introduce the gamedef intro():    # printing the introduction    print("""    As a result of the murder of a physicist, Father Silvano Bentivoglio,    a symbolist, Robert Langdon, and a scientist, Vittoria Vetra, embark on an    adventure involving a secret brotherhood, the Illuminati. Clues lead them     all around the Vatican City, including the four altars of science, Earth,    Air, Fire, and Water. An assassin, working for the Illuminati, has captured     four cardinals, and murdered three. Robert and Vittoria are also searching     for a cannister that is a new destructive weapon that could kill millions     of people if it explodes. \n\n    Your goal is to stop the cannister from exploding and killing millions of people and     finding the insider that gave the assassin access to the cardinals.\n    Good Luck!!!    """)    #Telling the user to continue when they are done reading    user_input = input(prompt = "<Press Enter to Continue>")    print(f""" {'*' * 60}                                           ____ _____  _    ____ _____   _  / ___|_   _|/ \  / ___| ____| / | \___ \ | | / _ \| |  _|  _|   | |  ___) || |/ ___ \ |_| | |___  | | |____/ |_/_/   \_\____|_____| |_|                                  {'*' * 60} """)    stage_1() #calling stage_1 function to start once the user continues.         #defining play again function for stage_2 to call it later.#This will be called each time the user lose stage_2. They will try stage_2 again.def play_again_2():        count_invalid_input = 3 #adding a counter to control the invalid inputs    while count_invalid_input > 0: #condition for the while loop to run                user_input = input(prompt = f"""        Do you want to try again?\n        1. yes        2. no """)                #converting user_input to lower case and no spaces        user_input = user_input.lower().strip()        if '1' in user_input or 'yes' in user_input or 'y' in user_input :            stage_2() #calling stage_2 function if the user wants to try again            break #breaking the loop                        #end the game if they user doesn't want to continue        elif '2' in user_input or 'no' in user_input or 'n' in user_input :            print('Thanks for playing. Have a nice day!!!')            end()            break                        #telling the user they entered an invalid answer        else:            print("\n Invalid response!!! Please enter yes/no \n")            count_invalid_input -= 1 #invalid_input decreases by 1 each time the user enters invalid answer                        if count_invalid_input != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {count_invalid_input} attempt(s) left""")            else:                end()                break                        #defining elements function in order to call it instead of repeating the entire codedef elements():        print ("""    There is a clue in the page "let the angels guide thee on thy lofty quest"\n    We need to figure out the first element in the altar of science, where the      statue of an angel will point us to the direction of the church of illumination.\n             """)    #declaring list of elements    elements = [['1','air'],['2','fire'],['3','water'], ['4','earth']]        #random.shuffle(elements)        attempt = 3 #setting limit for the number of attempts        passed = False # bolean test for while loop to run            while attempt > 0: #setting a condition for the loop to run                for element in elements: #using a for loop to print the list            print(f"""    {element[0]}.{element[1]} """)                    #Making the game unpredictable by creating random choice        choice = random.choices(elements)                    user_input = input(prompt = """    Choose the first element from the list: """)                               #controlling user_input to have lower case and no spaces        user_input = user_input.lower().strip()           # 2nd while Start ----------------------------------------        while True: #using a 2nd while loop to check conditions            if user_input in choice[0]:                print(f"""    Fantastic!!!! {choice[0]} is the first element!!!                 """)                win() #calling win function when the user pick the right element                print(f""" {'*' * 60}      ____ _____  _    ____ _____   _____  / ___|_   _|/ \  / ___| ____| |___ /  \___ \ | | / _ \| |  _|  _|     |_ \   ___) || |/ ___ \ |_| | |___   ___) | |____/ |_/_/   \_\____|_____| |____/                                                              {'*' * 60}""")                passed = True                              #elif statement for when the user doesnt pick the correct element or enters an invalid answer               elif user_input not in choice[0] :                attempt -= 1 # the number of attempts decreases by 1 each time they enter invalid input                 #telling the user how many attempts they have left                print(f"""    That is not the first element. Try again!! You have {attempt} attempt(s) left\n""")                                          else:                 print("Invalid input!!! Try again")                attempt -= 1            break #breaking the 2nd while loop# 2nd while End -------------------------------------------# Checking if passed or not        if passed:            break #break the loop when they passed        else:             pass    if passed: #proceed to stage 3 when if passed        print("""    Follow the lead to the church of Illumination""")        stage_3() #calling stage 3            else:        print("You ran out of tries!!")         lose()        play_again_2()                # defining fight or run function so I can call it if the user enters invalid input. Forcing the user to play def fight_or_run ():        count_invalid_entries = 3  #adding a counter to control the invalid inputs        while count_invalid_entries > 0: #condition for the while loop to run                user_input = input(prompt = """    Enter a number    1. fight    2. run """)        user_input = user_input.lower().strip()        if 'run' in user_input or '2' in user_input or 'ru' in user_input:            print("Good choice!!!")            elements()            break         elif 'fight' in user_input or '1' in user_input or 'fig' in user_input or 'fi' in user_input:            print("""    Bad choice!!! The guard defeated you and took you to jail. """)            lose()            play_again_2()            break        else:            print("Invalid input!!! Enter fight or run")                        count_invalid_entries -=1#invalid_input decreases by 1 each time the user enters invalid answer            if count_invalid_entries != 0:                #telling the user how many attempts they have left                print(f"""    I was expecting fight or run (1 or 2). You have {count_invalid_entries} attempt(s) left""")            else:                end()                break                            def stage_2(): #defining stage_2 as a function    print( """    We are now in the Vatican Archives\n    We have 4 hours left before the cannister explodes.\n      We have the bound and there is a coded message in page 66. \n    Do you want to steal page 66 or steal the whole book? \n """)    count_incorrect_answer = 3 #adding a counter to control the invalid inputs        while count_incorrect_answer > 0:  #condition for the while loop to run                #getting the user to decide what to steal            user_input = input(prompt = """     Enter a number to decide    1. page    2. book  """)        #controlling user_input to have lower case and no spaces        user_input = user_input.lower().strip()        #if statement to decide the next move based on the user's choice        if 'page' in user_input or '1' in user_input or 'pa' in user_input:            print("""    Superb!!! You got away with the page""")            elements()            break                    # elif statement based on the user's choice                elif 'book' in user_input or '2' in user_input or 'bo' in user_input :            print ("""    Wrong move!!! The book triggered the alarm and the guard is after you.    Do you want to run or fight back?            """)            fight_or_run()            break                    #else statement to tell the user they have entered an invalid answer        else:            print('\nInvalid input!! Enter page or book to decicde:')                         count_incorrect_answer -= 1#invalid_input decreases by 1 each time the user enters invalid answer            if count_incorrect_answer != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting page or book (1 or 2). You have {count_incorrect_answer} attempt(s) left""")            else:                end()                break                     #defining a function to call it later to avoid writing it out againdef oli_or_carl():        invalid_user_input = 3 #adding a counter to control the invalid inputs        while invalid_user_input > 0: #condition for the while loop to run                user_input = input(prompt = """    Who could it be?\n    1.Olivetti    2.Carlo  """)                #converting user_input to capital letter        user_input = user_input.capitalize().strip()                #if statement for the user to lose if they enter Oliver        if 'Olivetti' in user_input or '1' in user_input or 'Oli' in user_input or 'Ol' in user_input:            print("""    Olivetti was a tough man but he was loyal. Nice Try!!!""")            lose() # calling the lose function            play_again_3_lose()            break        #elif statement to tell the user they won if they enter Carlo            elif 'Carlo' in user_input or '2' in user_input or 'Car' in user_input or 'Ca' in user_input:            print("""    You are right!!! The Camerlengo was the insider!!!     What a plot twist? You saved the people and solved the mystery!!! """)            win() #calling the win function            play_again_3_win()            break        #else statement if the user entertained an invalid answer            else:            print("Invalid input!!! Please enter Olivetti or Carlo (1 or 2)")                        invalid_user_input -= 1#invalid_input decreases by 1 each time the user enters invalid answer                        if invalid_user_input != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting Olivetti or Carlo (1 or 2). You have {invalid_user_input} attempt(s) left""")            else:                end()                break            #defining a function to call it later to avoid writing it out againdef cam_or_guard():        wrong_answer = 3 #adding a counter to control the invalid inputs        while wrong_answer > 0: #condition for the while loop to run                user_input = input("""    Enter camerlengo or guard\n    1. camerlengo    2. guard                                    """)        #converting the input to lower case and  no spaces        user_input = user_input.lower().strip()        #if statement to proceed to next step if the user chooose camerlengo        if 'camerlengo' in user_input or '1' in user_input or 'cam' in user_input or 'len' in user_input:            print("""    Good thinking!!! The camerlengo flew the cannister out of the city.    Luckily the camerlengo ejected out of the helicopter before the cannister exploded.                        """)                print("""    You saved the people!!!\n\n    Who do you think was the insider that gave the assasin access to the cardinals in the conclave?    Only The head of the swiss guard, Commander Olivetti and the Camerlengo Carlo had access to the conclave.                """)            oli_or_carl()            break        #elif user enters guard, end the game                elif 'guard' in user_input or '2' in user_input or 'gu' in user_input:            print("""    We ran out of time!!! The cannister exploded before the guard got there.""")            lose() #calling lose function            play_again_3_lose()            break        #else statement if the user enters an invalid answer          else:            print("""    Invalid input!! Please enter camerlengo or guard (1 or 2)""")                         wrong_answer -= 1#invalid_input decreases by 1 each time the user enters invalid answer            if wrong_answer != 0:                #telling the user how many attempts they have left                print(f"""    I was expecting camerlengo or guard (1 or 2). You have {wrong_answer} attempt(s) left""")            else:                end()                break                        #defining a function to call it later to avoid writing it out againdef fly_or_drive():        count_wrong_answer = 3 #adding a counter to control the invalid inputs        while count_wrong_answer > 0: #condition for the while loop to run                user_input = input("""    Great choice!! we can either drive the cannister out of the city or fly it    out, what do you want to do? fly or drive?    1. fly    2. drive """)        user_input = user_input.lower().strip()        if 'fly' in user_input or '1' in user_input or 'fl' in user_input :            print("""    Awesome!!!     The camerlengo can fly a helicopter and he has agreed to fly the cannister out.     We can either let him fly or wait for the Swiss Guard to come fly it out.            """)            cam_or_guard()            break        #elif statement to lose the game if the user enters drive                elif 'drive' in user_input or '2' in user_input or 'dri' in user_input:            print("""     You couldn't make it in time out of the city. The cannister exploded!!!             """)                        lose()#calling lose function            play_again_3_lose()            break        #else statement to try again if the user enters invalid answer           else:            print("""    Invalid input!!! Please enter fly or drive""")                        count_wrong_answer -= 1#invalid_input decreases by 1 each time the user enters invalid answer                        if count_wrong_answer != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting fly or drive (1 or 2). You have {count_wrong_answer} attempt(s) left""")            else:                end()                break    def bat_or_evac():        count_invalid_user_input = 3        while count_invalid_user_input > 0:                user_input = input("""    Enter battery or evacuate     1. battery    2. evacuate                       """)        #converting answe to lower case and no spaces        user_input = user_input.lower().strip()        if 'battery' in user_input or '1' in user_input or 'bat' in user_input:            print("""    Not smart!! The cannister exploded before you could replace the battery.""")            lose() #calling lose function            play_again_2()            break        elif 'evacuate' in user_input or '2' in user_input or 'eva' in user_input or 'evac' in user_input:            fly_or_drive()            break        # else statement to try again if the user enters invalid answer                        else:            print("invalid input!!! Enter battery or evacuate")                        count_invalid_user_input -= 1#invalid_input decreases by 1 each time the user enters invalid answer            if count_invalid_user_input != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting battery or evacuate (1 or 2). You have {count_invalid_user_input} attempt(s) left""")            else:                end()                break                            def want_to_cont():        incorrect_attempt = 3        while incorrect_attempt > 0:        user_input = input(prompt = """    Do you want to continue? (yes/no)\n    1. yes    2. no  """)        #converting user_input to lower case and no spaces        user_input = user_input.lower().strip()        if 'yes' in user_input or '1' in user_input or 'y' in user_input :            print("""    We found the cannister!!!     However, we have just 30 mins till the cannister explodes.    We can either try to change the cannister battery or take the cannister out of the Vatican City.        What do we do? \n""")            bat_or_evac()            break        #elif statement to end the game if the user doesnt want to continue               elif 'no' in user_input or 'n' in user_input  or '2' in user_input:            print("""    Oh dear!!! Too bad the cannister will explode killing millions.""")            end()            break        #else statement to try again if the user enters invalid answer            else:            print("\nInvalid input!!! Please enter yes or no")                        incorrect_attempt -= 1#invalid_input decreases by 1 each time the user enters invalid answer                        if incorrect_attempt != 0:                                #telling the user how many attempts they have left                print(f"""    I was expecting yes or no (1 or 2). You have {incorrect_attempt} attempt(s) left""")            else:                end()                break                def stage_3():        print( """        We made it to The Church of Illumination!!!     We managed to save one of the kidnapped cardinals.    Let's find the cannister, we have just 1 hour left before the cannister explodes.    """)    dice() #calling dice function to start stage_3    intro() #calling intro function to start the game